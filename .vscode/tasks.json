{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile Protobuf",
            "type": "shell",
            "command": "mkdir -p proto/gen && protoc -I=proto --go_out=proto/gen --go_opt=paths=source_relative --go-grpc_out=proto/gen --go-grpc_opt=paths=source_relative kv.proto",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "reveal": "always",
                "panel": "new"
            },
            "problemMatcher": []
        }
    ]
}